<form method="post" action="{{ path('app_herbier_delete', {'id': herbier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ herbier.id) }}">
    <button class="btn">Delete</button>
</form>
